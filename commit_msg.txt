🧪 Add tests for WakeOnLan.WakeAsync happy paths

🎯 **What:** The testing gap in `WakeOnLan.WakeAsync` happy path has been addressed. The existing tests only verified validation failures (null, empty strings, invalid format).
📊 **Coverage:** A new `Theory` test `WakeAsync_ValidMacAddress_ReturnsTrue` was added to verify valid MAC addresses (with colons, hyphens, dots, and no separators) parse correctly and execute successfully.
✨ **Result:** Increased code coverage by testing the main valid operation branches in the code.
