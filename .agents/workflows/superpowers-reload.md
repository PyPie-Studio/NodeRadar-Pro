---
description: Reloads and re-indexes the agent skills, rules, and knowledge graph.
---

# Superpowers Reload

## Procedure
1. Re-index `.agents/rules/` and `.agents/skills/`.
2. Update the codebase AST graph (`graphify update .`).
3. Verify MCP server health.
